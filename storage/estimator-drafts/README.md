File-backed estimator draft storage lives here at runtime.

- `estimator-drafts.jsonl` is created automatically by the app.
- Records include session ID, estimator inputs, route recommendation, risk tier, and attribution values.
- This directory exists so estimator behavior can be tied back to later lead submissions.
